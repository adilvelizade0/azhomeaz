import styled from "styled-components";

const AboutWrapper = styled.div`
  p {
    font-size: 1rem;
    line-height: 1.5;
    font-weight: 500;
    color: #555555;
  }
`;

export default AboutWrapper;
